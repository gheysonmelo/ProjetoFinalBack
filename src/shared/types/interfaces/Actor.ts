export interface ActorInput {
    first_name: string,
    last_name: string,
};

export interface ActorOutput {
    actor_id: number,
    first_name: string,
    last_name: string,
    last_update: Date,
};